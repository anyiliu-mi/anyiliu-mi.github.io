import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://anyiliu-mi.github.io',
    base: '/anyiliu.github.io/',
    trailingSlash: 'ignore',
});
